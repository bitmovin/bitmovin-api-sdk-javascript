/**
 * Specifies whether to use path or virtual-hosted style access
 * @export
 * @enum {string}
 */
export enum S3AccessStyle {
  VIRTUAL_HOSTED = 'VIRTUAL_HOSTED',
  PATH = 'PATH'
}

export default S3AccessStyle;

