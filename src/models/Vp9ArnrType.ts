/**
 * altref noise reduction filter type
 * @export
 * @enum {string}
 */
export const enum Vp9ArnrType {
  BACKWARD = 'BACKWARD',
  FORWARD = 'FORWARD',
  CENTERED = 'CENTERED'
}

export default Vp9ArnrType;

