/**
 * ESAM direction indicator following the SCTE-250 standard
 * @export
 * @enum {string}
 */
export enum EsamDirection {
  OUT = 'OUT',
  IN = 'IN',
  BOTH = 'BOTH'
}

export default EsamDirection;

