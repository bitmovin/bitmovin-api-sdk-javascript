/**
 * Defines the mode for weighted prediction for P-frames
 * @export
 * @enum {string}
 */
export const enum WeightedPredictionPFrames {
  DISABLED = 'DISABLED',
  SIMPLE = 'SIMPLE',
  SMART = 'SMART'
}

export default WeightedPredictionPFrames;

