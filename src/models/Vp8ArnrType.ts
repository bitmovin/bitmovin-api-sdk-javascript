/**
 * altref noise reduction filter type
 * @export
 * @enum {string}
 */
export const enum Vp8ArnrType {
  BACKWARD = 'BACKWARD',
  FORWARD = 'FORWARD',
  CENTERED = 'CENTERED'
}

export default Vp8ArnrType;

