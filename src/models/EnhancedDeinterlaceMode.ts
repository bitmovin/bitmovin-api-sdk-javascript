/**
 * Specifies the method how fields are converted to frames
 * @export
 * @enum {string}
 */
export enum EnhancedDeinterlaceMode {
  FRAME = 'FRAME',
  FIELD = 'FIELD'
}

export default EnhancedDeinterlaceMode;

