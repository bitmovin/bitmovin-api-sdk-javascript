/**
 * Which instrument established the picture, where anything did.
 * @export
 * @enum {string}
 */
export enum PccHdrConfidence {
  EVIDENCE = 'evidence',
  CLAIM = 'claim',
  UNESTABLISHED = 'unestablished'
}

export default PccHdrConfidence;

