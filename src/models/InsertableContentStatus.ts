/**
 * @export
 * @enum {string}
 */
export enum InsertableContentStatus {
  CREATED = 'CREATED',
  DOWNLOADING = 'DOWNLOADING',
  READY = 'READY',
  ERROR = 'ERROR'
}

export default InsertableContentStatus;

