/**
 * Mode of clock synchronization for ad insertion
 * @export
 * @enum {string}
 */
export enum ClockSynchronizationMode {
  SYSTEM_CLOCK = 'SYSTEM_CLOCK',
  EMBEDDED = 'EMBEDDED'
}

export default ClockSynchronizationMode;

