/**
 * Variant stream dropping mode.
 * @export
 * @enum {string}
 */
export const enum VariantStreamDroppingMode {
  STREAM = 'STREAM',
  AUDIO = 'AUDIO'
}

export default VariantStreamDroppingMode;

