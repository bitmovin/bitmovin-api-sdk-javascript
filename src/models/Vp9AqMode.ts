/**
 * Adaptive quantization mode.
 * @export
 * @enum {string}
 */
export enum Vp9AqMode {
  NONE = 'NONE',
  VARIANCE = 'VARIANCE',
  COMPLEXITY = 'COMPLEXITY',
  CYCLIC = 'CYCLIC'
}

export default Vp9AqMode;

