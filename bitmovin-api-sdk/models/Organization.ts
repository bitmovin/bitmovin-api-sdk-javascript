import {map} from '../common/Mapper';
import BitmovinResource from './BitmovinResource';

/**
 * @export
 * @class Organization
 */
export default class Organization extends BitmovinResource {
  constructor(obj: any) {
    super(obj);
  }

}
