import {map} from '../common/Mapper';
import BitmovinResource from './BitmovinResource';

/**
 * @export
 * @class Group
 */
export default class Group extends BitmovinResource {
  constructor(obj: any) {
    super(obj);
  }

}
